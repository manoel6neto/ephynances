<%= form_for(@agreement_document) do |f| %>
  <% if @agreement_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agreement_document.errors.count, "error") %> prohibited this agreement_document from being saved:</h2>

      <ul>
      <% @agreement_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
