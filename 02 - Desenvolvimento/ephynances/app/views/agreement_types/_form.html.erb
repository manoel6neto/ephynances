<%= form_for(@agreement_type) do |f| %>
  <% if @agreement_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agreement_type.errors.count, "error") %> prohibited this agreement_type from being saved:</h2>

      <ul>
      <% @agreement_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
